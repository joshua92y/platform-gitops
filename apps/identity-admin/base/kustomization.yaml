# apps/identity-admin/base/ — deployment·service·ingress·configmap·externalsecret-env·
# externalsecret-migrate·migrate-job (Ingress host = PLACEHOLDER.joshuatech.dev).
# 계약 gitops-repo.md §디렉터리. 빈 뼈대(T003) — 실제 매니페스트는 T031+에서 작성한다.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
