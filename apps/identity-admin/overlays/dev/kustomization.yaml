# apps/identity-admin/overlays/dev/ — namespace(jt-dev)·images[].digest(태그 금지)·
# Ingress host JSON6902·replicas. 계약 gitops-repo.md §디렉터리·§이미지·승격.
# dev digest bump는 joshuatech-gitapp-1[bot]의 auto-merge PR로만 바뀐다.
# 빈 뼈대(T003) — 실제 내용은 T031+에서 작성한다.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
