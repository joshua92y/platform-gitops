# bootstrap/argocd/ — remote base(install.yaml, `?ref=<commit sha>` 핀 — 태그 금지) + patches
# (dex·applicationset 비활성, requests, ServerSideApply). 계약 gitops-repo.md §디렉터리.
# 빈 뼈대(T003) — 실제 내용은 T031+에서 작성한다.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
