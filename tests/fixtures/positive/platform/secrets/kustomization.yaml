# 긍정 픽스처 — platform/secrets(`secrets/<ns>`의 배달자). 검사 7.3: `secrets/` 아래를 base로 가지는 유일한 kustomization이고,
# 이 트리의 `secrets/vault/`가 여기에 포함돼 있어야 한다(포함되지 않으면 죽은 선언으로 FAIL)
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../secrets/vault
