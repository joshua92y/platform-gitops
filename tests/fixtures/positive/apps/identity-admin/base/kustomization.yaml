# 긍정 픽스처 — apps/<pod>/base (계약 gitops-repo.md §디렉터리). 값은 전부 가짜.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - externalsecrets.yaml
  - migrate-job.yaml
