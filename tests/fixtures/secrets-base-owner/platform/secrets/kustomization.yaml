# 부정 픽스처 7.3 WAVE-secrets-base — 배달자 본인(이 파일만 `secrets/` 아래를 base로 가질 수 있다)
# 이 트리에는 secrets/ 아래 ns가 둘인데 여기에는 하나만 있다 → secrets/data/ 가 "죽은 선언"으로 FAIL해야 한다
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../secrets/cert-manager
