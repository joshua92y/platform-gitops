# projects/ — AppProject 4종 자리 (계약 gitops-repo.md §Application 규약)

`platform.yaml` · `dev.yaml` · `prod.yaml` · `tests.yaml`(+ `default` 비우기)을 T031+에서 작성한다. `tests`는 `platform/policies/tests/` 검증 Job 3종 전용(destination `jt-dev`만).
